(library
 (name translate)
 (modules env config llm_client schema prompts)
 (libraries yojson lwt lwt.unix cohttp cohttp-lwt cohttp-lwt-unix))

; The 2.2 live smoke test: spends real (tiny) money, needs the key — run by
; hand with `dune exec translate/smoke.exe`, never wired into `dune test`.

(executable
 (name smoke)
 (modules smoke)
 (libraries translate))
